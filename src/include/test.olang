fn printHello() {
  print("Hello");
}

fn addTwoNumbers(a: int, b: int) {
  return a + b;
}

fn avg(lst: list) {
  let i: int;
  let sum = 0;

  for i = 0; i < len(lst); ++i {
    sum += lst[i];
  }

  return sum / len(lst);
}