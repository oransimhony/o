fn printHello() {
  print "Hello";
}

fn addTwoNumbers(a, b) {
  return a + b;
}

fn avg(lst) {
  let i;
  let sum = 0;

  for i = 0; i < len(lst); ++i {
    sum += lst[i];
  }

  return sum / len(lst);
}